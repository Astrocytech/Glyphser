# Known Limitations (Milestone 1)

- Validation scope is limited to the locked initial operator subset in the milestone plan.
- PyTorch CPU package is installed in `.venv`, but lockfile pin integration process remains to be finalized in project dependency workflow.
