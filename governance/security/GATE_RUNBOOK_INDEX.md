# Gate Runbook Index

This index maps each security gate to triage metadata.

## `security_toolchain_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_toolchain_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_toolchain_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_toolchain_lock_signature_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_toolchain_lock_signature_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_toolchain_lock_signature_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_toolchain_reproducibility_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_toolchain_reproducibility_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_toolchain_reproducibility_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_scan_ordering_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_scan_ordering_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_scan_ordering_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `mypy_security_profile_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/mypy_security_profile_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/mypy_security_profile_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `ruff_security_profile_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/ruff_security_profile_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/ruff_security_profile_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_gate_onboarding_checklist_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_gate_onboarding_checklist_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_gate_onboarding_checklist_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `subprocess_allowlist_report.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/subprocess_allowlist_report.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/subprocess_allowlist_report.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `subprocess_direct_usage_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/subprocess_direct_usage_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/subprocess_direct_usage_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_workflow_contract_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_workflow_contract_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_workflow_contract_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `workflow_risky_patterns_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/workflow_risky_patterns_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/workflow_risky_patterns_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `workflow_deprecated_invocation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/workflow_deprecated_invocation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/workflow_deprecated_invocation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `policy_schema_validation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/policy_schema_validation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/policy_schema_validation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_schema_compatibility_policy_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_schema_compatibility_policy_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_schema_compatibility_policy_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_schema_migration_tracker.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_schema_migration_tracker.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_schema_migration_tracker.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_schema_strict_readiness_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_schema_strict_readiness_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_schema_strict_readiness_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_evidence_corruption_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_evidence_corruption_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_evidence_corruption_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `unicode_confusable_detection_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/unicode_confusable_detection_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/unicode_confusable_detection_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_artifact_signature_coverage_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_artifact_signature_coverage_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_artifact_signature_coverage_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_unsigned_json_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_unsigned_json_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_unsigned_json_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_workflow_trigger_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_workflow_trigger_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_workflow_trigger_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_critical_test_wiring_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_critical_test_wiring_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_critical_test_wiring_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_sarif_permissions_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_sarif_permissions_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_sarif_permissions_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_workflow_permissions_policy_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_workflow_permissions_policy_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_workflow_permissions_policy_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_exception_suppression_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_exception_suppression_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_exception_suppression_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_dead_gate_wiring_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_dead_gate_wiring_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_dead_gate_wiring_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `hardening_todo_consistency_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/hardening_todo_consistency_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/hardening_todo_consistency_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `governance_markdown_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/governance_markdown_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/governance_markdown_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `runbook_index_health_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/runbook_index_health_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/runbook_index_health_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `review_policy_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/review_policy_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/review_policy_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `incident_regression_catalog_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/incident_regression_catalog_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/incident_regression_catalog_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `high_severity_incident_regression_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/high_severity_incident_regression_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/high_severity_incident_regression_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `file_permissions_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/file_permissions_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/file_permissions_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `policy_signature_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/policy_signature_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/policy_signature_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `pip_audit_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/pip_audit_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/pip_audit_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `bandit_baseline_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/bandit_baseline_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/bandit_baseline_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `secret_scan_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/secret_scan_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/secret_scan_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `workflow_pinning_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/workflow_pinning_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/workflow_pinning_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `workflow_linter_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/workflow_linter_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/workflow_linter_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `incident_response_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/incident_response_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/incident_response_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `org_secret_backend_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/org_secret_backend_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/org_secret_backend_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `secret_management_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/secret_management_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/secret_management_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `secret_origin_inventory_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/secret_origin_inventory_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/secret_origin_inventory_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `secret_rotation_metadata_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/secret_rotation_metadata_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/secret_rotation_metadata_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `stale_secret_usage_wiring_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/stale_secret_usage_wiring_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/stale_secret_usage_wiring_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `break_glass_secret_usage_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/break_glass_secret_usage_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/break_glass_secret_usage_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `offline_network_call_guard_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/offline_network_call_guard_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/offline_network_call_guard_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `production_controls_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/production_controls_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/production_controls_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `runtime_api_state_schema_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/runtime_api_state_schema_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/runtime_api_state_schema_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `abuse_telemetry_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/abuse_telemetry_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/abuse_telemetry_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `temporary_waiver_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/temporary_waiver_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/temporary_waiver_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `provenance_revocation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/provenance_revocation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/provenance_revocation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `report_secret_leak_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/report_secret_leak_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/report_secret_leak_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_gate_test_coverage_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_gate_test_coverage_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_gate_test_coverage_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `third_party_pentest_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/third_party_pentest_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/third_party_pentest_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `live_integrations_verify.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/live_integrations_verify.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/live_integrations_verify.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `live_rollout_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/live_rollout_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/live_rollout_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `container_provenance_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/container_provenance_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/container_provenance_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `evidence_attestation_index.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/evidence_attestation_index.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/evidence_attestation_index.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `evidence_attestation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/evidence_attestation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/evidence_attestation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `provenance_signature_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/provenance_signature_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/provenance_signature_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `integrity_envelope_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/integrity_envelope_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/integrity_envelope_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `key_provenance_continuity_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/key_provenance_continuity_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/key_provenance_continuity_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `key_rotation_cadence_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/key_rotation_cadence_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/key_rotation_cadence_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `signature_algorithm_policy_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/signature_algorithm_policy_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/signature_algorithm_policy_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_verification_summary.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_verification_summary.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_verification_summary.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_cross_gate_consistency_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_cross_gate_consistency_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_cross_gate_consistency_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_actionable_findings_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_actionable_findings_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_actionable_findings_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `telemetry_completeness_sli_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/telemetry_completeness_sli_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/telemetry_completeness_sli_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `telemetry_timeliness_sli_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/telemetry_timeliness_sli_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/telemetry_timeliness_sli_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `telemetry_integrity_sli_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/telemetry_integrity_sli_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/telemetry_integrity_sli_gate.json` and `evidence/security/telemetry_integrity_trend_alert.json`.
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `slsa_attestation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/slsa_attestation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/slsa_attestation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `workflow_evidence_scope_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/workflow_evidence_scope_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/workflow_evidence_scope_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `conformance_security_coupling_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/conformance_security_coupling_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/conformance_security_coupling_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `key_management_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/key_management_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/key_management_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `cosign_attestation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/cosign_attestation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/cosign_attestation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `release_rollback_provenance_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/release_rollback_provenance_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/release_rollback_provenance_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `emergency_lockdown_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/emergency_lockdown_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/emergency_lockdown_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `evidence_chain_of_custody.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/evidence_chain_of_custody.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/evidence_chain_of_custody.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `evidence_chain_of_custody.py", "--verify"`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/evidence_chain_of_custody.py", "--verify"`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/evidence_chain_of_custody.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_slo_report.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_slo_report.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_slo_report.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_trend_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_trend_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_trend_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_dashboard_export.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_dashboard_export.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_dashboard_export.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_schema_normalization_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_schema_normalization_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_schema_normalization_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_baseline_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_baseline_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_baseline_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `crypto_agility_matrix_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/crypto_agility_matrix_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/crypto_agility_matrix_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `provenance_witness_quorum_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/provenance_witness_quorum_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/provenance_witness_quorum_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `transparency_log_export.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/transparency_log_export.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/transparency_log_export.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `transparency_log_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/transparency_log_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/transparency_log_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `time_source_attestation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/time_source_attestation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/time_source_attestation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `build_environment_drift_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/build_environment_drift_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/build_environment_drift_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `artifact_classification_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/artifact_classification_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/artifact_classification_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `deterministic_redaction_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/deterministic_redaction_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/deterministic_redaction_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `disaster_recovery_drill_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/disaster_recovery_drill_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/disaster_recovery_drill_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `key_compromise_dual_control_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/key_compromise_dual_control_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/key_compromise_dual_control_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `dependency_trust_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/dependency_trust_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/dependency_trust_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_workflow_evidence_bundle_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_workflow_evidence_bundle_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_workflow_evidence_bundle_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `replay_abuse_regression_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/replay_abuse_regression_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/replay_abuse_regression_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `exception_registry_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/exception_registry_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/exception_registry_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `lockdown_blast_radius_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/lockdown_blast_radius_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/lockdown_blast_radius_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `canonical_json_roundtrip_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/canonical_json_roundtrip_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/canonical_json_roundtrip_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `file_permission_matrix_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/file_permission_matrix_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/file_permission_matrix_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `tamper_canary_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/tamper_canary_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/tamper_canary_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `security_docs_traceability_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/security_docs_traceability_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/security_docs_traceability_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `sbom_diff_review_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/sbom_diff_review_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/sbom_diff_review_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `egress_policy_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/egress_policy_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/egress_policy_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `attestation_freshness_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/attestation_freshness_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/attestation_freshness_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `crypto_algorithm_policy_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/crypto_algorithm_policy_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/crypto_algorithm_policy_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `split_duty_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/split_duty_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/split_duty_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `export_offline_verify_bundle.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/export_offline_verify_bundle.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/export_offline_verify_bundle.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `external_export_data_minimization_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/external_export_data_minimization_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/external_export_data_minimization_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `exported_security_evidence_api_contract_versioning_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/exported_security_evidence_api_contract_versioning_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/exported_security_evidence_api_contract_versioning_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `dependency_confusion_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/dependency_confusion_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/dependency_confusion_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `deterministic_env_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/deterministic_env_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/deterministic_env_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `archive_integrity_revalidation_gate.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/archive_integrity_revalidation_gate.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/archive_integrity_revalidation_gate.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `compromised_runner_drill.py`
Purpose: Enforce security control compliance for this gate.
Inputs: `tooling/security/compromised_runner_drill.py`, governance policies, and run-scoped evidence artifacts.
Outputs: `evidence/security/compromised_runner_drill.json` (or gate-specific report).
Fail reasons: Missing prerequisites, policy violations, integrity mismatches, stale or malformed evidence.
Triage steps: Re-run gate locally, inspect report findings, fix root cause, regenerate signed evidence if required.

## `governance_compliance_delta_report.py`
Purpose: Auto-generate a compliance delta between required governance controls and current repository state.
Inputs: `governance/security/governance_compliance_baseline.json`, repository files, and optional evidence JSON status artifacts.
Outputs: `evidence/security/governance_compliance_delta_report.json`.
Fail reasons: Missing or invalid baseline policy (delta findings produce `WARN`, not hard-fail).
Triage steps: Restore/fix baseline schema, then address reported control deltas until summary reaches full compliance.

## `governance_compliance_quarterly_snapshot.py`
Purpose: Emit a signed quarter-scoped governance compliance snapshot derived from the latest compliance delta report.
Inputs: `evidence/security/governance_compliance_delta_report.json`, repository signing key context.
Outputs: `evidence/security/governance_compliance_quarterly_snapshot.json` and `.sig`.
Fail reasons: Missing/invalid compliance delta source or invalid snapshot signature.
Triage steps: Regenerate compliance delta report, ensure signing configuration is available, and rerun snapshot generation.

## `gate_status_transition_anomaly_detector.py`
Purpose: Detect suspicious gate-status transitions (for example chronic WARN/FAIL runs followed by sudden PASS) as tamper signals.
Inputs: `evidence/security/*.json` gate reports and `evidence/security/gate_status_transition_history.json`.
Outputs: `evidence/security/gate_status_transition_anomaly_detector.json` and updated history snapshot.
Fail reasons: This detector emits `WARN` on suspicious transitions and continues execution to preserve forensic signal.
Triage steps: Validate whether the transition was expected (real remediation) or suspicious (tamper/workflow bypass), then investigate affected gates.

## `cross_run_suspicious_pattern_linkage_gate.py`
Purpose: Link repeated suspicious telemetry/evidence findings across runs to surface multi-run tamper patterns.
Inputs: `evidence/security/*.json` findings and `evidence/security/cross_run_suspicious_pattern_history.json`.
Outputs: `evidence/security/cross_run_suspicious_pattern_linkage_gate.json` and updated cross-run history counts.
Fail reasons: This correlation gate emits `WARN` when suspicious patterns repeat beyond threshold.
Triage steps: Investigate repeated signals for shared root cause or tamper campaign indicators and escalate incident response if persistent.

## `containment_auto_trigger_recommendations.py`
Purpose: Emit containment auto-trigger recommendations when severe findings appear in gate outputs.
Inputs: `evidence/security/*.json` findings.
Outputs: `evidence/security/containment_auto_trigger_recommendations.json` with per-signal recommended containment actions.
Fail reasons: This recommendation layer emits `WARN` when severe findings are observed.
Triage steps: Apply recommended containment actions (promotion block, integrity revalidation, runner compromise drill) based on matched signal type.

## `incident_severity_scoring_report.py`
Purpose: Score incident findings using a defined severity rubric and publish normalized severity metrics in security reports.
Inputs: `evidence/security/*.json` findings.
Outputs: `evidence/security/incident_severity_scoring_report.json`.
Fail reasons: Emits `WARN` when high/critical severity findings are present.
Triage steps: Prioritize remediation by severity score and route critical/high findings through incident response workflow.

## `evidence_notarization_checkpoint.py`
Purpose: Verify a signed notarization receipt binds to the immutable long-term retention manifest digest.
Inputs: `evidence/security/long_term_retention_manifest.json`, `evidence/security/evidence_notarization_receipt.json(.sig)`.
Outputs: `evidence/security/evidence_notarization_checkpoint.json`.
Fail reasons: Missing manifest/receipt/signature, invalid receipt signature, or digest mismatch.
Triage steps: Regenerate/restore notarization receipt, ensure receipt signature validity, and re-bind receipt digest to current manifest.

## `manifest_pointer_meta_chain.py`
Purpose: Maintain an immutable chain of pointers to run-bundle manifests with chained digest continuity.
Inputs: `evidence/security/long_term_retention_manifest.json`, existing `manifest_pointer_meta_chain.json`.
Outputs: `evidence/security/manifest_pointer_meta_chain.json` and `.sig`.
Fail reasons: Missing source manifest, invalid prior chain structure, or chain signature verification failure.
Triage steps: Restore source retention manifest, repair malformed chain entries, and regenerate signed chain head.

## `archival_encryption_verification_gate.py`
Purpose: Enforce archival encryption-at-rest policy and key-custody metadata verification evidence.
Inputs: `governance/security/archival_encryption_policy.json`, evidence file referenced by `verification_evidence_path`.
Outputs: `evidence/security/archival_encryption_verification_gate.json`.
Fail reasons: Missing policy fields, invalid custody metadata, missing verification evidence, or unverified evidence status.
Triage steps: Complete policy metadata, publish current encryption verification evidence with timestamp, and rerun gate.

## `quarterly_restore_simulation.py`
Purpose: Simulate quarterly restore drills on randomly selected historic runs and validate restored evidence integrity.
Inputs: `evidence/runs/*/*/security/long_term_retention_manifest.json(.sig)` and referenced restored evidence files.
Outputs: `evidence/security/quarterly_restore_simulation.json`.
Fail reasons: Missing historical runs, missing/invalid signed manifests, or restored evidence digest mismatches.
Triage steps: Repair archived bundles/manifests, rerun restore simulation, and document corrective actions for failed sampled runs.

## `developer_mode_profile_gate.py`
Purpose: Enforce secure developer-mode profile semantics: strict checks stay enabled and mock attestations are explicit/allowlisted.
Inputs: `governance/security/developer_mode_profile.json`, developer-mode environment variables.
Outputs: `evidence/security/developer_mode_profile_gate.json`.
Fail reasons: Missing strict env flags, missing explicit mock attestations, or unapproved mock attestation identifiers.
Triage steps: Enable required strict flags, provide explicit mock attestation list, and keep mocks within approved policy allowlist.

## `promotion_go_no_go_report.py`
Purpose: Produce aggregate weighted promotion decision (`GO`/`NO_GO`) with mandatory blocker enforcement.
Inputs: `governance/security/promotion_go_no_go_policy.json`, referenced evidence reports.
Outputs: `evidence/security/promotion_go_no_go_report.json`.
Fail reasons: Mandatory controls not `PASS`, weighted score below threshold, WARN category thresholds exceeded per environment, or invalid policy/control configuration.
Triage steps: Resolve mandatory blockers first, reduce over-threshold WARN categories for target environment, then improve weighted control outcomes until threshold is met.

## `signed_promotion_decision_artifact.py`
Purpose: Emit signed promotion decision artifact with exact bypassed blockers and explicit approval chain.
Inputs: `evidence/security/promotion_go_no_go_report.json`, `GLYPHSER_BYPASSED_BLOCKERS`, `GLYPHSER_APPROVAL_CHAIN`.
Outputs: `evidence/security/promotion_decision_artifact.json(.sig)` and `signed_promotion_decision_artifact_gate.json`.
Fail reasons: Missing go/no-go report, bypassed blockers not present in source findings, insufficient approval chain, or signature validation failure.
Triage steps: Align bypass list with real blockers, provide required approver chain, regenerate signed decision artifact.

## `release_handshake_artifact.py`
Purpose: Require explicit release handshake acknowledgments from both security and release owners before release promotion.
Inputs: `GLYPHSER_SECURITY_OWNER`, `GLYPHSER_RELEASE_OWNER`, `GLYPHSER_RELEASE_HANDSHAKE_SECURITY_ACK`, `GLYPHSER_RELEASE_HANDSHAKE_RELEASE_ACK`.
Outputs: `evidence/security/release_handshake_artifact.json(.sig)` and `release_handshake_artifact_gate.json`.
Fail reasons: Missing owner identity, missing explicit acknowledgment from either role, or signature validation failure.
Triage steps: Confirm responsible owners, provide explicit acknowledgments for both roles, then regenerate and verify signed handshake artifact.

## `post_release_verification_checklist_artifact.py`
Purpose: Emit a signed post-release checklist artifact proving mandatory security uploads are present and readable.
Inputs: Security artifact directory (`evidence/security`) and optional `GLYPHSER_MANDATORY_SECURITY_UPLOADS`.
Outputs: `evidence/security/post_release_verification_checklist_artifact.json(.sig)` and `post_release_verification_checklist_artifact_gate.json`.
Fail reasons: Missing mandatory artifact, unreadable artifact, invalid JSON in required JSON artifact, or signature validation failure.
Triage steps: Regenerate missing artifacts, fix malformed outputs, rerun checklist generation, and re-upload verified artifacts.

## `rollback_handshake_artifact.py`
Purpose: Emit a signed rollback handshake artifact proving policy/provenance revalidation succeeded before rollback completion.
Inputs: `evidence/security/provenance_signature.json`, `evidence/security/policy_signature.json`, `evidence/security/release_rollback_provenance_gate.json`.
Outputs: `evidence/security/rollback_handshake_artifact.json(.sig)` and `rollback_handshake_artifact_gate.json`.
Fail reasons: Missing revalidation artifacts, non-`PASS` revalidation status, or signature validation failure.
Triage steps: Re-run provenance and policy signature gates, ensure rollback provenance gate passes, then regenerate signed rollback handshake artifact.

## `release_notes_security_status_gate.py`
Purpose: Ensure release notes include a security control status summary aligned with promotion decision state.
Inputs: Release notes markdown (`GLYPHSER_RELEASE_NOTES_PATH`, default `CHANGELOG.md`), `promotion_go_no_go_report.json`.
Outputs: `evidence/security/release_notes_security_status_gate.json`.
Fail reasons: Missing release notes, missing security summary section, or missing `GO/NO_GO` decision reference.
Triage steps: Update release notes with explicit security status summary and promotion decision mapping.

## `release_metadata_evidence_checksum.py`
Purpose: Embed reproducible evidence-package checksum into release metadata.
Inputs: Security evidence JSON artifacts in `evidence/security/`.
Outputs: `evidence/security/release_metadata.json` and `release_metadata_evidence_checksum_gate.json`.
Fail reasons: Embedded checksum mismatch after write/verify cycle or malformed metadata payload.
Triage steps: Regenerate release metadata and confirm deterministic evidence package entry set for target release.

## `lockfile_transitive_provenance_gate.py`
Purpose: Validate transitive provenance hardening for security tooling lockfile entries via hash-pinned dependencies.
Inputs: `requirements.lock`.
Outputs: `evidence/security/lockfile_transitive_provenance_gate.json`.
Fail reasons: Missing security tool entries or missing `--hash=sha256:` provenance pins for required tools.
Triage steps: Regenerate lockfile with hash pins and ensure security tools are present with transitive integrity metadata.

## `toolchain_binary_source_consistency_gate.py`
Purpose: Verify installed security tooling artifact hashes match lockfile-pinned hashes (binary/source consistency).
Inputs: `tooling/security/security_toolchain_lock.json`, `evidence/security/security_toolchain_install_report.json`.
Outputs: `evidence/security/toolchain_binary_source_consistency_gate.json`.
Fail reasons: Missing install report observations or mismatched observed hash vs pinned lock hash.
Triage steps: Reinstall toolchain via locked bootstrap, refresh install report, and investigate hash drift before promotion.

## `package_metadata_anomaly_gate.py`
Purpose: Enforce package metadata anomaly policy for suspicious maintainer/email/domain drift.
Inputs: `governance/security/package_metadata_anomaly_policy.json`, dependency registry snapshot metadata.
Outputs: `evidence/security/package_metadata_anomaly_gate.json`.
Fail reasons: Disallowed maintainers/domains detected or expected metadata drift for monitored packages.
Triage steps: Validate upstream package ownership metadata, update expected metadata with approved review, and block suspicious package changes.

## `mirrored_source_integrity_report.py`
Purpose: Compare package hashes across mirrored source fetches and detect source-integrity divergence.
Inputs: `evidence/security/security_toolchain_install_report.json`.
Outputs: `evidence/security/mirrored_source_integrity_report.json`.
Fail reasons: Same package/version fetched from multiple sources with differing hashes.
Triage steps: Block affected toolchain update, investigate mirror/source tamper, and restore trusted source parity.

## `chaos_security_scenario_report.py`
Purpose: Validate security chaos scenarios (network install outage fail-safe, partial upload detection, corrupted signature detection).
Inputs: Relevant gate outputs in `evidence/security/` (toolchain gate, upload manifest gate, signature/attestation gates).
Outputs: `evidence/security/chaos_security_scenario_report.json`.
Fail reasons: Scenario evidence present but expected fail-safe/detection behavior not observed.
Triage steps: Fix failing scenario controls and rerun chaos scenario validations; missing evidence yields `WARN` until scenario execution artifacts are produced.

## `gate_retry_strategy_policy_gate.py`
Purpose: Enforce gate retry strategy policy (allowed gates, max attempts, deterministic retry constraints).
Inputs: `governance/security/gate_retry_strategy_policy.json`, retry events evidence file.
Outputs: `evidence/security/gate_retry_strategy_policy_gate.json`.
Fail reasons: Retry on disallowed gate, attempts above max policy, or missing deterministic seed metadata.
Triage steps: Restrict retries to approved gates, cap attempts per policy, and ensure deterministic seeded retry execution.

## `branch_protection_policy_gate.py`
Purpose: Verify repository branch-protection required checks/review rules remain aligned with policy and workflow job wiring.
Inputs: `.github/branch-protection.required.json`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`.
Outputs: `evidence/security/branch_protection_policy.json`.
Fail reasons: Missing required checks/jobs, critical policy checks missing, or required workflow jobs absent.
Triage steps: Restore required status/release checks in policy and ensure mapped workflow jobs remain present.

## `codeowners_security_coverage_gate.py`
Purpose: Ensure CODEOWNERS coverage remains complete for security-critical paths defined by policy.
Inputs: `governance/security/review_policy.json`, `.github/CODEOWNERS`.
Outputs: `evidence/security/codeowners_security_coverage_gate.json`.
Fail reasons: Missing CODEOWNERS file or missing coverage entries for required security-critical paths.
Triage steps: Add/restore CODEOWNERS patterns for all required security-critical paths and re-run gate.

## `permissions_audit_artifact.py`
Purpose: Produce periodic permissions audit artifact for repository/workflow token scopes.
Inputs: `.github/workflows/*.yml` permissions blocks and optional `GLYPHSER_REPO_PERMISSION_SCOPES` metadata.
Outputs: `evidence/security/permissions_audit_artifact.json`.
Fail reasons: Emits `WARN` on risky permissions patterns (for example `write-all`) while preserving full audit snapshot.
Triage steps: Reduce permissions scopes to least privilege and track periodic scope audits for governance evidence.

## `emergency_access_review_gate.py`
Purpose: Enforce emergency-access review controls with expiry bounds and attested closure requirements.
Inputs: `governance/security/emergency_access_policy.json`, `governance/security/emergency_access_grants.json`.
Outputs: `evidence/security/emergency_access_review_gate.json`.
Fail reasons: Active grants expired/exceed max duration or closed grants missing closure attestation fields.
Triage steps: Close or revoke expired emergency access immediately and record signed closure attestation metadata for closed grants.

## `threat_control_mapping_gate.py`
Purpose: Validate threat-to-control mappings, owners, workflow wiring, and critical gate ownership coverage.
Inputs: `governance/security/threat_control_matrix.json`, `governance/security/metadata/THREAT_MODEL.meta.json`, `tooling/security/security_super_gate_manifest.json`, workflow files.
Outputs: `evidence/security/threat_control_mapping_gate.json`.
Fail reasons: Unmapped controls, missing owners, missing gate/workflow references, critical gates without mapped owners, metadata/matrix drift.
Triage steps: Update threat control matrix and threat metadata IDs, wire missing gates in workflows, ensure owners and evidence paths are complete.

## `threat_model_change_attestation_gate.py`
Purpose: Require signed change attestation whenever critical threat-control IDs are modified.
Inputs: `governance/security/threat_control_matrix.json`, `governance/security/metadata/THREAT_MODEL_CHANGE_ATTESTATION.json(.sig)`, git diff metadata.
Outputs: `evidence/security/threat_model_change_attestation_gate.json`.
Fail reasons: Critical control changed without attestation, missing/invalid attestation signature, missing approvers/rationale, attestation missing impacted controls.
Triage steps: Create/update signed threat model change attestation with impacted critical controls, approvers, and rationale; re-run gate.

## `security_gate_dependency_graph_gate.py`
Purpose: Build a dependency graph for all super-gate listed security controls and detect dependency cycles or single-point verifier risks for critical controls.
Inputs: `tooling/security/security_super_gate_manifest.json`, `governance/security/security_gate_dependency_policy.json`.
Outputs: `evidence/security/security_gate_dependency_graph.json`, `evidence/security/security_gate_dependency_graph_gate.json`.
Fail reasons: Unknown gates/dependencies in policy, cyclic dependencies, or critical controls with fewer redundant verifiers than policy requires.
Triage steps: Correct dependency policy references, break dependency cycles, and add backup verifier coverage for critical controls.

## `critical_control_backup_verifier_gate.py`
Purpose: Enforce primary/backup verifier behavior for critical controls so unavailable primary checks can use approved backups while fail-closed controls do not fail open.
Inputs: `governance/security/critical_control_backup_policy.json`, required control reports under `evidence/security/`.
Outputs: `evidence/security/critical_control_backup_verifier_gate.json`.
Fail reasons: Invalid control policy entries, fail-closed primary verifier failures, or unavailable primary control reports without any passing backup verifier.
Triage steps: Fix primary control failures, restore missing control report generation, or add approved backup verifiers in policy for unavailable controls.

## `critical_control_redundancy_report.py`
Purpose: Produce a resilience report for critical control verifier redundancy and highlight single-verifier bottlenecks.
Inputs: `governance/security/security_gate_dependency_policy.json`, `governance/security/critical_control_backup_policy.json`.
Outputs: `evidence/security/critical_control_redundancy_report.json`.
Fail reasons: Critical controls have fewer available verifiers than required redundancy policy thresholds.
Triage steps: Add independent backup verifiers for critical controls and update dependency/backup policy entries to restore required redundancy.

## `envelope_signature_design_checklist_gate.py`
Purpose: Ensure multi-signer envelope signature design checklist requirements stay defined and complete.
Inputs: `governance/security/ENVELOPE_SIGNATURE_MULTI_SIGNER_CHECKLIST.md`.
Outputs: `evidence/security/envelope_signature_design_checklist_gate.json`.
Fail reasons: Missing checklist, missing required design requirements, or incomplete checklist coverage.
Triage steps: Update the checklist with all required multi-signer design controls and verify all required checklist items are present.

## `crypto_parameter_review_gate.py`
Purpose: Enforce periodic cryptographic parameter review policy freshness and minimum security thresholds.
Inputs: `governance/security/crypto_parameter_policy.json`, optional `GLYPHSER_PROVENANCE_HMAC_KEY`.
Outputs: `evidence/security/crypto_parameter_review_gate.json`.
Fail reasons: Weak/allowlisted forbidden hashes, stale review date, invalid review policy fields, or runtime HMAC key below configured minimum.
Triage steps: Update crypto parameter policy review, tighten minimum key/hash settings, and rotate runtime keys to meet minimum policy requirements.

## `runner_integrity_fingerprint_gate.py`
Purpose: Enforce policy allowlisting of runner OS/toolchain fingerprints for trusted security execution contexts.
Inputs: `governance/security/runner_integrity_fingerprint_policy.json`, runtime env (`GLYPHSER_ENV`, `RUNNER_OS`, Python version).
Outputs: `evidence/security/runner_integrity_fingerprint_gate.json`.
Fail reasons: Current CI/release runner fingerprint does not match any policy-approved fingerprint.
Triage steps: Update runner integrity policy for approved environments or remediate unexpected runner/toolchain drift.

## `workflow_env_injection_gate.py`
Purpose: Detect unexpected environment variable injection in workflow definitions by enforcing policy allowlisted env variable patterns.
Inputs: `.github/workflows/*.yml`, `governance/security/workflow_env_var_policy.json`.
Outputs: `evidence/security/workflow_env_injection_gate.json`.
Fail reasons: Workflow env keys outside allowlisted patterns.
Triage steps: Remove injected env variables or update policy allowlist only after explicit security review.

## `trusted_execution_context_gate.py`
Purpose: Enforce trusted execution context policy by workflow type (allowed runner class and required environment markers).
Inputs: `.github/workflows/*.yml`, `governance/security/trusted_execution_context_policy.json`.
Outputs: `evidence/security/trusted_execution_context_gate.json`.
Fail reasons: Disallowed workflow runner context or missing/mismatched required context environment variables.
Triage steps: Move workflow jobs to trusted runner contexts and align required execution environment markers with policy.

## `untrusted_pr_secret_isolation_gate.py`
Purpose: Produce verification evidence that untrusted pull-request code paths cannot access protected secrets without explicit fork guards.
Inputs: `.github/workflows/*.yml`.
Outputs: `evidence/security/untrusted_pr_secret_isolation_gate.json`.
Fail reasons: Pull-request workflows reference secrets without explicit non-fork guard conditions.
Triage steps: Add fork-aware guard conditions before secret usage or move secret-dependent steps to trusted non-PR contexts.

## `security_lineage_map_artifact.py`
Purpose: Build a deterministic lineage map linking security report fields to source artifacts and component reports.
Inputs: `evidence/security/*.json`.
Outputs: `evidence/security/security_lineage_map.json`.
Fail reasons: Invalid report JSON blocks lineage mapping generation.
Triage steps: Repair malformed security report artifacts and re-run lineage mapping.

## `security_super_gate_membership_guard_gate.py`
Purpose: Prevent silent shrinkage of required core/extended super-gate membership.
Inputs: `tooling/security/security_super_gate_manifest.json`, `governance/security/security_super_gate_membership_baseline.json`.
Outputs: `evidence/security/security_super_gate_membership_guard_gate.json`.
Fail reasons: Missing required core/extended gate entries or missing baseline/manifest.
Triage steps: Restore required manifest entries or intentionally update baseline policy with review and signature updates.

## `security_super_extended_compare_gate.py`
Purpose: Classify failed super-gate checks into core vs extended categories and detect unknown failures.
Inputs: `evidence/security/security_super_gate.json`, `tooling/security/security_super_gate_manifest.json`.
Outputs: `evidence/security/security_super_extended_compare_gate.json`.
Fail reasons: Missing source reports/manifests or failures that cannot be classified by manifest.
Triage steps: Ensure the super-gate report exists, keep manifest aligned, and remediate unknown gate references.

## `security_report_schema_contract_gate.py`
Purpose: Validate the formal versioned security report schema contract used by gates.
Inputs: `governance/security/security_report_schema_contract.json`.
Outputs: `evidence/security/security_report_schema_contract_gate.json`.
Fail reasons: Missing contract, invalid required field set, invalid schema version, missing required status values.
Triage steps: Restore contract file, keep required field set stable, and bump schema version only with approved migration docs.

## `security_retention_policy_gate.py`
Purpose: Enforce baseline retention policy structure for immutable security evidence storage.
Inputs: `governance/security/security_retention_policy.json`.
Outputs: `evidence/security/security_retention_policy_gate.json`.
Fail reasons: Missing policy, missing required fields, invalid booleans, empty storage/retention fields.
Triage steps: Update retention policy to include all required fields and signed policy coverage.

## `degraded_mode_evidence.py`
Purpose: Emit explicit evidence whenever non-blocking fallback/override behaviors are used.
Inputs: Security report artifacts (for example `promotion_policy_gate.json`, `key_provenance_continuity_gate.json`) and runtime override env flags.
Outputs: `evidence/security/degraded_mode_evidence.json`.
Fail reasons: This artifact is observational (`PASS`/`WARN`) and captures degraded-mode events rather than failing closed.
Triage steps: Review fallback events, reduce override use, and convert temporary fallback paths to explicit secure configuration.

## `independent_verifier_profile_gate.py`
Purpose: Validate read-only independent verifier command profile and forbidden command pattern protections.
Inputs: `governance/security/independent_verifier_profile.json`.
Outputs: `evidence/security/independent_verifier_profile_gate.json`.
Fail reasons: Missing profile, invalid role, missing allowed commands, forbidden patterns present in allowed commands.
Triage steps: Restrict verifier profile to read-only commands and remove unsafe command patterns.

## `offline_verification_gate.py`
Purpose: Verify policy signature manifest and chain-of-custody checks without network dependencies.
Inputs: `governance/security/policy_signature_manifest.json`, policy signatures, `evidence/security/evidence_chain_of_custody.json`.
Outputs: `evidence/security/offline_verification_gate.json`.
Fail reasons: Missing manifest, missing/invalid policy signatures, or chain-of-custody verification failures.
Triage steps: Regenerate signatures with approved key, restore manifest coverage, and rerun chain-of-custody generation/verification.

## `file_mode_change_gate.py`
Purpose: Detect suspicious file mode transitions in `.github/workflows/**` and `tooling/security/**` and require signed approval metadata when chmod-style drift is introduced.
Inputs: Git diff summary (`HEAD~1..HEAD`), `governance/security/file_mode_change_approval.json`, and `governance/security/file_mode_change_approval.json.sig`.
Outputs: `evidence/security/file_mode_change_gate.json`.
Triage steps: Confirm whether mode transitions were intentional, update the signed approval file with exact `path/old_mode/new_mode` tuples, and re-run the gate.

## `network_endpoint_documentation_gate.py`
Purpose: Detect newly introduced network endpoint/domain references in security scripts and policy JSON files that are not documented in the signed endpoint inventory.
Inputs: `tooling/security/**/*.py`, `governance/security/*.json`, `governance/security/network_endpoint_inventory.json`, and `governance/security/network_endpoint_inventory.json.sig`.
Outputs: `evidence/security/network_endpoint_documentation_gate.json`.
Triage steps: For each `undocumented_network_endpoint:*` finding, either remove the endpoint usage or add the endpoint to the signed inventory and record review context.

## `security_rule_baseline_gate.py`
Purpose: Protect security rule baselines by requiring a signed approval record tied to the exact git diff hash whenever semgrep/bandit rule files change.
Inputs: Git diff for `tooling/security/semgrep-rules.yml` and `tooling/security/bandit.yaml`, plus `governance/security/security_rule_diff_approval.json` and signature.
Outputs: `evidence/security/security_rule_baseline_gate.json`, `evidence/security/security_rule_baseline_diff.txt`.
Triage steps: Validate intent of the rule changes, update approval metadata with a fresh `rule_diff_sha256`, resign approval file, and rerun the gate.

## `security_fixture_integrity_gate.py`
Purpose: Detect tampering or undeclared additions in security-critical test fixtures by verifying signed baseline hashes.
Inputs: `governance/security/security_fixture_integrity_baseline.json(.sig)` plus monitored fixture roots (semgrep fixtures, security corpus fixtures, security incident replay fixture).
Outputs: `evidence/security/security_fixture_integrity_gate.json`.
Triage steps: Investigate any hash mismatch or undeclared fixture file, restore trusted fixture content, then regenerate and sign baseline only after approved fixture updates.

## `stale_security_fixture_gate.py`
Purpose: Flag stale fixture data when fixture files lag behind gate logic updates beyond an allowed threshold.
Inputs: `tooling/security/stale_security_fixture_policy.json` plus tracked gate scripts and fixture paths.
Outputs: `evidence/security/stale_security_fixture_gate.json`.
Triage steps: For each stale fixture finding, refresh the fixture set to match current gate behavior (or intentionally adjust threshold/mapping), then rerun the gate.

## `oncall_triage_sla_drill_gate.py`
Purpose: Prove on-call readiness by validating signed drill artifacts for top recurring security failures against triage SLA targets.
Inputs: `governance/security/oncall_triage_sla_policy.json(.sig)` and `governance/security/oncall_triage_sla_drill.json(.sig)`.
Outputs: `evidence/security/oncall_triage_sla_drill_gate.json`.
Triage steps: For any SLA breach or insufficient sample finding, rerun drills for affected failure classes, update operational playbooks, and refresh signed drill artifacts.

## `runbook_command_health_gate.py`
Purpose: Validate that runbook-referenced operational commands still execute successfully in the current environment.
Inputs: `governance/security/runbook_command_health_checks.json(.sig)` command list.
Outputs: `evidence/security/runbook_command_health_gate.json`.
Triage steps: For each failed command, repair invocation/environment prerequisites, update runbooks if command contracts changed, and rerun gate.

## `fork_pr_sarif_skip_simulation_gate.py`
Purpose: Produce runbook simulation evidence that SARIF uploads are skipped for fork PRs while raw Semgrep artifacts remain available.
Inputs: `.github/workflows/ci.yml` fork condition and SARIF/artifact step markers.
Outputs: `evidence/security/fork_pr_sarif_skip_simulation_gate.json`.
Triage steps: Restore fork-aware SARIF guard condition and artifact upload fallback in CI workflow, then rerun simulation gate.
